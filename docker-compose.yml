version: '3.8'

services:
  intranet:
    image: office-intranet:latest
    container_name: office-intranet
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - FLASK_SECRET_KEY=***REMOVED***
      - JWT_SECRET_KEY=***REMOVED***
    volumes:
      - intranet_data:/app/backend/data
    restart: unless-stopped

volumes:
  intranet_data:

